import type { GroupType } from '../types/graph';

export const groupTypeLabelMap: Record<GroupType, string> = {
  virtualNetwork: 'Virtual Network',
  subnet: 'Subnet',
  logicalGroup: 'Logical Group',
};
